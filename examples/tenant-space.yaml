apiVersion: core.oam.dev/v1beta1
kind: Application
metadata:
  name: tenant-space-demo
spec:
  components:
    - name: platform-space
      type: tenant-space
      properties:
        tenantName: platform-services
        displayName: Platform services
