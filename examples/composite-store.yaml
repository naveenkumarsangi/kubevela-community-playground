apiVersion: core.oam.dev/v1beta1
kind: Application
metadata:
  name: composite-store-demo
spec:
  components:
    - name: orders
      type: composite-store
      properties:
        storeName: orders
        accessPoints: 2
